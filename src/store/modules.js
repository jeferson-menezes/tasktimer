import { store as task } from '@/views/task'
import { store as home } from "@/views/home";
import { store as history } from "@/views/history";
import { store as config } from '@/views/config';


export default {
    task,
    home,
    history,
    config
}